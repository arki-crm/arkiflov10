# ============================================
# ARKIFLO - Root Environment Variables
# ============================================
# Copy this file to .env and fill in your values
# ============================================

# ============ MONGODB CREDENTIALS ============
# Root admin (for database administration)
MONGO_ROOT_USER=admin
MONGO_ROOT_PASSWORD=CHANGE_THIS_ROOT_PASSWORD_123

# Application user (used by backend)
# This user is created in ADMIN database with readWrite on arkiflo
MONGO_APP_USER=arkiflo_app
MONGO_APP_PASSWORD=CHANGE_THIS_APP_PASSWORD_456

# ============ SEED ADMIN USER ============
# First admin user created on startup (for login)
SEED_ADMIN_EMAIL=admin@yourcompany.com
SEED_ADMIN_PASSWORD=YourSecurePassword123!

# ============ GOOGLE OAUTH (Direct - No Emergent) ============
# Get these from Google Cloud Console: https://console.cloud.google.com/apis/credentials
# 1. Create OAuth 2.0 Client ID (Web application type)
# 2. Add authorized redirect URI: http://YOUR_SERVER_IP/api/auth/google/callback
GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=YOUR_GOOGLE_CLIENT_SECRET

# ============ FRONTEND ============
# Your server's public URL (NO trailing slash)
REACT_APP_BACKEND_URL=http://YOUR_SERVER_IP

# ============================================
# DEPLOYMENT STEPS:
# 1. Copy this file: cp .env.example .env
# 2. Change ALL passwords above
# 3. Set up Google OAuth (see GOOGLE_OAUTH_SETUP.md)
# 4. Set your server IP in REACT_APP_BACKEND_URL
# 5. Run: docker compose up -d --build
# 6. Login with Google OR SEED_ADMIN_EMAIL / SEED_ADMIN_PASSWORD
# ============================================
